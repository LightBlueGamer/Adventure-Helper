import { Attributes } from '../src';

describe('Attributes', () => {
  describe('is a class', () => {
    test('typeof Attributes returns function', () => {
      expect(typeof Attributes).toBe('function');
    });
  });
});
