import { Rarity } from '../src';

describe('Rarity', () => {
  describe('is a class', () => {
    test('typeof Rarity returns function', () => {
      expect(typeof Rarity).toBe('function');
    });
  });
});
