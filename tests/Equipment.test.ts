import { Equipment } from '../src';

describe('Equipment', () => {
  describe('is a class', () => {
    test('typeof Equipment returns function', () => {
      expect(typeof Equipment).toBe('function');
    });
  });
});
