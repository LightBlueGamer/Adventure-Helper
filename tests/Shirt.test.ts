import { Shirt } from '../src';

describe('Shirt', () => {
  describe('is a class', () => {
    test('typeof Shirt returns function', () => {
      expect(typeof Shirt).toBe('function');
    });
  });
});
