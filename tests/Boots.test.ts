import { Boots } from '../src';

describe('Boots', () => {
  describe('is a class', () => {
    test('typeof Boots returns function', () => {
      expect(typeof Boots).toBe('function');
    });
  });
});
