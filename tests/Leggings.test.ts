import { Leggings } from '../src';

describe('Leggings', () => {
  describe('is a class', () => {
    test('typeof Leggings returns function', () => {
      expect(typeof Leggings).toBe('function');
    });
  });
});
