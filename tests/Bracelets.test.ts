import { Bracelets } from '../src';

describe('Bracelets', () => {
  describe('is a class', () => {
    test('typeof Bracelets returns function', () => {
      expect(typeof Bracelets).toBe('function');
    });
  });
});
