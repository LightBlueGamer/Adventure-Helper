import { Chestplate } from '../src';

describe('Chestplate', () => {
  describe('is a class', () => {
    test('typeof Chestplate returns function', () => {
      expect(typeof Chestplate).toBe('function');
    });
  });
});
