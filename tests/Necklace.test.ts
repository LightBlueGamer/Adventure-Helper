import { Necklace } from '../src';

describe('Necklace', () => {
  describe('is a class', () => {
    test('typeof Necklace returns function', () => {
      expect(typeof Necklace).toBe('function');
    });
  });
});
