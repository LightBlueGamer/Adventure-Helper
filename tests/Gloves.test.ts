import { Gloves } from '../src';

describe('Gloves', () => {
  describe('is a class', () => {
    test('typeof Gloves returns function', () => {
      expect(typeof Gloves).toBe('function');
    });
  });
});
