import { Offhand } from '../src';

describe('Offhand', () => {
  describe('is a class', () => {
    test('typeof Offhand returns function', () => {
      expect(typeof Offhand).toBe('function');
    });
  });
});
